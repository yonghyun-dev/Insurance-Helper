# 성능 기록 (Performance Log)

> **규칙**: 파이프라인(파싱·청킹·검색·OCR/IE)·응답 품질에 영향 주는 변경마다 여기에
> **예전 방식 → 변경 → 실측 결과**를 기록한다. 좋아진 것만이 아니라 **동등·악화도 정직하게** 남긴다.
> 재현 커맨드를 함께 적어 다음 측정과 비교 가능하게 유지한다. (최신이 위)

---


## 2026-07-21 — 헤더/푸터 fix 재인제스트 (라이브 반영) + before/after — 지표 중립 (PM-43)

커밋된 헤더/푸터 죽은코드 복구(시그니처 정규화, 1d3d55d)를 로컬 인덱스에 재인제스트로 반영
(`ica rebuild` 5사, 백업 후). 골든셋 v2(45) before/after 비교(가중 0.02 동일):

| | BEFORE(재인제스트 전) | AFTER |
|---|---|---|
| hit@8 | 0.844 | **0.844 (동일)** |
| mrr@8 | 0.589 | 0.575 (−0.014, 노이즈) |
| ndcg@8 | 0.649 | 0.644 |
| 골든-코퍼스 정합 | 45/45 | **45/45** |
| per-item 변화 | — | 개선 1(samsung 주사 7→6) · 악화 3(전부 ±1~2 랭크) |

- **결론: 검색 지표상 중립**. hit@8 불변, mrr 은 4문항 ±1 흔들림(노이즈). 헤더/푸터 fix 는
  **코드 정합(죽은코드 복구)으로는 옳으나 실제 검색 이득 ~0%** — 페이지번호는 임베더가 저살리언스로
  거의 무시하던 토큰. "헤더/푸터만으로 재인제스트는 ROI 낮다"는 판단이 데이터로 확인됨.
- keep: 로컬 인덱스를 커밋 코드와 일치시키는 게 맞음(롤백 시 코드↔인덱스 불일치가 노이즈보다 나쁨).
  인덱스(app.db/chroma)는 gitignore라 커밋 대상 아님. 청크 2504→2505.
- **구조 인식 부채(clause_no 섹션 모호성)는 별개·대형**: 파서가 특약 제1조 재시작을 알면서도 섹션
  (제N관/특별약관)을 추적 안 해 clause_no 충돌 → 골든 확장을 막은 근본. 섹션 감지+스키마 필드+
  마이그레이션+골든 동시수정이 필요한 Sprint 규모 → 별도 스코프. (헤더/푸터 재인제스트가 중립이었으므로
  이 대형 작업의 검색 이득도 사전 불확실 — 착수 시 동일 before/after 프로토콜 적용.)

## 2026-07-21 — RRF 심볼릭 가중 홀드아웃 재검증 (골든셋 v2 45문항) — 과적합 확인 (PM-43)

기존 `rag_symbolic_weight=0.1` 은 30문항 그리드서치로 확정("0.1 이 hit@8 +0.034"). 확장된
45문항(대표성↑)으로 가중 스윕 재실행 → **0.1 은 30문항 과적합이었음이 데이터로 확인**.

| 심볼릭 가중 | hit@3 | hit@8 | mrr@8 | ndcg@8 |
|---|---|---|---|---|
| **0.0 (뉴럴 단독)** | 0.711 | **0.844** | **0.588** | **0.652** |
| 0.05 | 0.711 | 0.844 | 0.562 | 0.631 |
| 0.1 (현행) | 0.711 | 0.844 | 0.575 | 0.639 |
| 0.15 | 0.711 | 0.844 | 0.563 | 0.626 |
| 0.3 | 0.689 | 0.844 | 0.521 | 0.605 |
| 0.5 | 0.689 | 0.822 | 0.517 | 0.597 |
| 1.0 | 0.556 | 0.778 | 0.463 | 0.523 |

- **뉴럴 단독(0.0)이 hit@8·mrr@8 모두 최고**. 현행 0.1 은 hit@8 동일·mrr 소폭 하락(0.588→0.575).
- **per-item(0.0 vs 0.1)**: 심볼릭이 **개선 2건**(hanwha 도수 2→1, hyundai 사기 5→1) vs **악화 7건**
  (주사·**별표/붙임 2건(hanwha 비급여 6→8, hyundai 신체부위 1→2)**·면책표·청약철회 등 전부 순위 강등).
- **핵심 반전**: 30문항 채택 근거였던 "심볼릭이 별표/붙임 케이스 회수"가 **현 인덱스에서 재현 안 됨**
  (오히려 별표 케이스 악화). 재인제스트(2505청크)로 인덱스가 바뀐 뒤 심볼릭 RRF 융합의 순효과가
  중립~소폭 음(-)으로 전환.
- **주의(범위 구분)**: 이 결과는 **검색 시점 RRF 융합 가중**에 한정. 심볼릭 채널의 다른 역할(coverage
  룰 엔진의 결정론 판정, 그래프 형제 확장)은 별개이며 뉴로심볼릭 **판정**의 핵심은 유지된다.
- **미세 스윕(0 근처)로 최적 확정**: hit@8 은 0~0.3 완전 평탄(0.844). mrr@8 은 **[0.0~0.03]가 평탄 최적**
  (0.588~0.589, 차이 0.001=1문항 미만 노이즈), **0.04↑ 뚜렷이 악화**(0.564→0.10=0.575→...→1.0=0.463).
  | 가중 | 0.0 | 0.02 | 0.03 | 0.04 | 0.1(구) | 0.5 |
  |---|---|---|---|---|---|---|
  | mrr@8 | 0.588 | **0.589** | 0.589 | 0.564 | 0.575 | 0.517 |
- **확정: `rag_symbolic_weight` 0.1 → 0.02** (측정 argmax·평탄대 내 저가중=견고). hit@8 손실 0, mrr +0.014
  vs 구 0.1. 심볼릭 융합을 끄지 않아 뉴로심볼릭 **검색** 서사 유지, 판정(coverage 룰)은 별개로 그대로.
  게이트(0.80/0.55/1.0) 통과. 재현: `RAG_SYMBOLIC_WEIGHT=x python -m eval.retrieval_metrics`.

## 2026-07-21 — 검색 골든셋 확장 v1(30) → v2(45) + 회귀 게이트 재보정 (PM-43)

| | v1 (30문항) | v2 (45문항) |
|---|---|---|
| 구성 | 5사×6, 커버리지/한도 중심(도수치료·MRI·주사·면책) | + 계약절차 3주제×5사(청약철회·고지위반·사기계약) |
| hit@8 | 0.867 | **0.844** |
| mrr@8 | 0.666 | **0.575** |
| hit@3 | — | 0.711 |
| filter_integrity | 1.0 | **1.0** |

- **정직한 하락**: v1 은 쉬운 커버리지 질의에 편중돼 실제보다 후하게 나왔다. 절차/계약 질의를 넣으니
  검색이 그 축에서 더 약함이 드러남(신규 15문항 자체는 hit@8 0.867 이나 mrr 는 낮아 랭킹이 뒤). → 개선 타깃.
- **게이트 재보정(회귀 감지용)**: 0.85/0.60(v1 편중 기준) → **0.80/0.55**(v2 대표 베이스라인 바로 아래).
  aspirational 목표가 아니라 회귀 방지선.
- **골든셋 확장 한계(중요 발견)**: 자동 발굴을 절차 주제로 넓히려 했으나 **코퍼스 clause_no 메타 모호성**이
  발목을 잡음 — 실손 약관은 여러 섹션(통칙·특약별)이 같은 조 번호를 재사용하고, 답변 본문이 교차참조·
  분산돼 있어 제목/앵커 자동매칭이 오답 조항을 잡는다. 그래서 **콘텐츠 앵커+제목 교차검증으로 정본 확인된
  3주제 15문항만** 채택(청약철회·고지위반·사기계약). 지급절차·납입연체·비례분담 등은 메타 정합 개선(구조
  인식 부채) 또는 손 큐레이션 필요 → 파킹랏. 각 문항 코퍼스 대조 검증(45/45 통과) + 실검색 실측 완료.
- 재현: `python -m eval.retrieval_metrics` (검증만: `--validate`).

## 2026-07-09 — Sprint 32 T3: 청크 메타 부착 + 소프트캡 재튜닝

| 항목 | 예전 | 지금 | 측정 |
|---|---|---|---|
| 청크 메타 | documents JOIN 으로만 (백엔드별 필터 정합 리스크) | clause_chunks 에 insurer_id/product_id/area/doc_type 비정규 부착 (alembic backfill + ingest write) | backfill 2505행 NULL 0 · 재인제스트 후 NULL 0 · pgvector 필터 컬럼 직독 전환 |
| ITEM 패킹 경계 | 초과 후 flush (1000→1500급 초과) | 초과 "전" flush | — |
| >1000tok 소프트 초과 | 557 | **556 (사실상 무변화)** | ⚠️ **정직 기록**: 모집단이 ITEM 패킹이 아니라 **항(paragraph) 단위 의미 청크**가 지배 — 실손 약관의 항이 본질적으로 큼. 의미 절단 없이 줄일 수 없어 **구조 특성으로 수용** (정확성 불변식 >3500=0 은 100% 유지) |

- 재인제스트 후: 3-스토어 정합 2504=2504=2504 (**ingest→그래프 자동 동기화 첫 실전 검증**),
  골든셋 hit@8 0.867 유지 · MRR 0.614(게이트 0.60 내) · 정합 30/30.

## 2026-07-09 — Sprint 32 T4: IE 열화 벤치 — 기본 경로를 데이터로 확정

| | 구 2단계 (OCR→Solar 재추출) | IE 1단계 (스키마 강제) |
|---|---|---|
| 필드 정확도 (20케이스×90필드) | 0.611 (55/90) | **1.000 (90/90)** |
| 평균 지연 | 2.7s | 5.2s |

- 벤치: 합성 서류 4종(진단서/영수증/청구서/사고확인원) × 열화 5조건(원본/저해상/회전5°/블러/관인) —
  `eval/ie_bench/` 생성기+러너로 재현 가능.
- 상세: 진단서·사고확인원은 양쪽 만점. **영수증(0/3)·청구서(2/6)는 구 경로가 열화 무관 전 조건 실패**
  (환자부담총액·증권번호·보험사 등 표 필드) — 열화 취약이 아니라 **텍스트 재추출 방식의 구조적 한계**.
- 판정: 이전 "깨끗한 문서 동등+더 느림" 기록을 뒤집는 결정적 근거 — **IE 기본 경로 확정**
  (+38.9%p, 지연 +2.5s 는 업로드 1회성 흐름에서 수용). 2단계는 폴백으로 유지.
- 재현: `python -m eval.ie_bench.generate && python -m eval.ie_bench.run`

## 2026-07-09 — Sprint 32 T2: 뉴로심볼릭 검색 (Memgraph 융합, 모드 폐지)

| | 예전 | 지금 |
|---|---|---|
| 아키텍처 | `rag_mode` 토글(vector/graph/hybrid) — hybrid 는 stub, graph 는 LLM-Cypher+수동적재라 사실상 죽은 채널(매 질의 헬스실패→폴백) | **단일 NeuroSymbolicRetriever** — 뉴럴(벡터)+심볼릭(그래프) 항상 병행, 가중 RRF 융합. 그래프 다운 시 뉴럴 단독 graceful(실측 검증) |
| 그래프 | Neo4j·LLM-Cypher·수동 graph-build·빈 그래프 | **Memgraph**(Bolt 호환)·**LLM 0회 결정론 Cypher**·ingest 자동 동기화·2505노드+**REFERS_TO 1511엣지**(1.5s 적재) |
| 우회 경로 | help/일반QA/agent 가 vectorstore.query 직행(점수컷·필터 미적용) | 전 경로 단일 파이프라인 경유 (우회 0) |

**골든셋 실측 (30문항, 전부 동일 인덱스)**
| 구성 | hit@3 | hit@8 | MRR@8 | nDCG@8 | 판정 |
|---|---|---|---|---|---|
| 베이스라인(벡터 단독) | 0.733 | 0.833 | 0.663 | 0.714 | 기준 |
| 융합 동가중(1.0) | 0.533 | 0.833 | 0.467 | 0.540 | ❌ 악화 — 심볼릭 노이즈가 뉴럴 정답 밀어냄 |
| 융합 w=0.3 | 0.700 | 0.867 | 0.573 | 0.666 | 부분 개선 |
| **융합 w=0.1 (채택)** | **0.733** | **0.867** | **0.666** | 0.712 | ✅ hit@8 +0.034, 나머지 동등 |
| 융합 w=0.1 + Solar 리랭크 | 0.500 | 0.833 | 0.513 | 0.595 | ❌ 리랭커 악화 — **기본 off 를 실측으로 확정** |

- 심볼릭 채널의 실효: 베이스라인 미스 5건 중 **별표/붙임 구조 2건(hanwha 응급증상, lotte 비급여) 회수** — REFERS_TO 확장의 직접 효과.
- 게이트 상향: hit@8 ≥0.80→**0.85**, MRR ≥0.55→**0.60** (개선 잠금).
- 재현: `ica eval-retrieval` (가중 그리드: `RAG_SYMBOLIC_WEIGHT=x python -m eval.retrieval_metrics`)

## 2026-07-09 — Sprint 32 T1: 검색 골든셋 30문항 + 회귀 하네스 (베이스라인 확정)

| | 예전 | 지금 |
|---|---|---|
| 평가 수단 | 스팟체크 3질의(비체계) + eval/runner **깨진 import 로 실행 불가** | 골든셋 30문항(5개사×6, 코퍼스 정합 자동검증 30/30) + `ica eval-retrieval` + pytest `-m eval` 게이트 |

**베이스라인 실측 (vector 단독, 2505청크 인덱스, top_k=8)**
| 지표 | 값 | 임계(게이트) |
|---|---|---|
| hit@3 | **0.733** | - |
| hit@8 | **0.833** | ≥0.80 |
| MRR@8 | **0.663** | ≥0.55 |
| nDCG@8 | **0.714** | - |
| 보험사 필터 정합 | **1.000** | =1.0 |

- 미스 5건: hanwha-MRI, hanwha-응급증상(별표), hyundai-갱신, meritz-주사료, lotte-비급여(별표)
  — **별표/붙임 구조 케이스 2건 포함** → T2 심볼릭(그래프 REFERS_TO 확장) 채널의 직접 타깃.
- 부수 발견: `.env rag_mode=hybrid` 상태라 매 질의 Neo4j 헬스 실패→vector 폴백 (죽은 채널 실측 확인 — T2 모드 폐지 근거).
- 재현: `.venv/bin/python -m app.interfaces.cli.app eval-retrieval` (정합만: `--validate`)

## 2026-07-09 — Sprint 31 D3: 사용자 서류 추출을 Upstage IE 로 전환

| | 예전 | 지금 |
|---|---|---|
| 방식 | OCR(텍스트) → PII 마스킹 → Solar 가 텍스트에서 슬롯 재추출 (**2단계**) | 분류 후 정형 4종은 원본 이미지+json_schema 를 IE 에 **1회 호출** (실패 시 기존 경로 폴백) |

**실측 (합성 진단서 900×1100 PNG, OCR conf 0.989)**
| 지표 | 구 2단계 | IE 1단계 | 판정 |
|---|---|---|---|
| 필드 정확도 (7필드) | **7/7** | **7/7** | 동등 |
| 추출 지연 | 3.7s | 6.4s | **IE 가 더 느림** |

- **정직한 결론**: 선명한 문서에선 정확도 우위 **미입증**, 지연은 오히려 손해. IE 의 기대 우위는
  저해상 촬영·도장·복잡 표 등 **실서류 열화 조건**이며 이는 아직 벤치 전. 폴백 구조라 리스크는 낮음.
- 부가 개선(측정 무관): 한글 날짜→ISO 정규화·정수형 보정이 스키마 강제로 결정론화,
  OCR 저신뢰(<0.6) `low_confidence` 재촬영 유도 신설.
- 재현: `tests` 외 수동 — 합성 진단서 생성 스크립트 + `adapter.extract_information` 직접 호출.

## 2026-07-09 — Sprint 31 D2: 검색 필터/정밀도 노브

| 항목 | 예전 | 지금 | 측정 |
|---|---|---|---|
| agent `search_terms` 필터 | **무필터** — rag_react=true 시 타 보험사 약관 인용 차단이 무효 (정합성 버그) | 슬롯 기반 area/insurer_id 필터 관통 | 정합성 복원 (버그픽스 — 수치화 대상 아님) |
| 점수 컷 | 없음 — 무관 청크도 top_k 채움 | `rag_score_ratio=0.55` (top1 대비 상대 컷) | **미측정** — 골든셋 후 튜닝 예정 |
| 리랭킹 | 없음 | `rag_rerank`(Solar listwise, 기본 off) | **미측정** — on/off A/B 예정 |

## 2026-07-09 — Sprint 31 D1: 약관 파싱·청킹 재작업 ★최대 개선

| | 예전 | 지금 |
|---|---|---|
| annex 인식 | `[별표N]` 만 — **`[붙임N]` 미인식** → 삼성 장해분류표 14p 가 직전 조항(제4조)에 뭉침 | 별표·붙임·별첨 + 괄호 뒤 무공백 인식 |
| 강제 분할 | 7000tok 에서 **토큰 단순 슬라이스** (문장/표 행 중간 절단) | 3500tok 에서 **라인 경계** 그리디(+80tok 오버랩) |
| 임베딩 정합 | 청크 7000tok vs 임베딩 절단 3800tok → **꼬리 미임베딩 = 검색 사각지대** | 청크 상한(3500) < 절단(3800) → 전 청크 전문 임베딩 보장 |
| ITEM 자식 | 분할 경로에서 **무시(유실)** — annex 인식 시 분류표 본문 소실 위험 | 연속 ITEM 그리디 패킹으로 보존 |

**실측 (5개사 실손 재인제스트, `ica rebuild`)**
| 지표 | 전 | 후 | 비고 |
|---|---|---|---|
| 임베딩 절단(>3800tok) 청크 | **34** (제3·4·12조 집중) | **0** | 핵심 불변식 확보 |
| annex 청크 | 9 | **48** (붙임 51건) | 분류표가 독립 검색 단위화 |
| 조항(clause_no) 인식률 | 94% | 95% | |
| 총 청크 | 2189 | 2505 | |
| >1000tok(소프트 상한 초과) | 294 | 557 | ⚠️ 증가 — 거대 청크(7000×소수)가 1000~1500tok 다수로 재분배된 결과. 정확성 불변식(≤3500)은 100% 충족, 소프트 상한 튜닝은 후속 |

**검색 스팟체크 (재인제스트 후, insurer=samsung)**
- "한 눈이 멀었을 때 장해지급률은 몇 퍼센트" → **붙임3 지급률 표 top1 (score 0.599)** — 해당 내용은
  이전 인덱스에서 임베딩 절단 꼬리(구조적으로 검색 불가 영역)에 있었음
- "도수치료 연간 보상한도와 공제금액" → 제3조 한도 표 top1 (0.563)
- "보상하지 않는 사항 미용 성형" → 제4조 3건 (0.524~)
- ⚠️ 구 인덱스는 in-place 재빌드로 소실 — 전/후 동일 질의 A/B 는 불가, 전 상태 근거는 구조 지표(절단 34건)로 갈음.

재현: `.venv/bin/python -m app.interfaces.cli.app verify` +
`sqlite3 app.db "select count(*) from clause_chunks where token_count>3800"` + 스팟체크 스크립트(vectorstore.query 3질의).

## 2026-07-08 — Sprint 30: 다중 실손 가입현황-우선 플로우

- 예전: 상황 입력 → 곧바로 판정 직행, 가입보험 `ins[0]` 1건 하드코딩.
- 지금: 본인확인 → **가입현황(다건 표시+선택)** → 상황 → 판정. 다건 시 비례분담 결정론 배너.
- 측정: 기능성 변경 (플로우 e2e Playwright 확인, 정량 지표 비대상).

## ~2026-07-08 — 이전 세션 주요 개선 (요약 소급)

- **슬롯 OR-그룹** (입원일수/통원횟수): "입원 안 했어요" 가 LLM 추출 실패 시 **무한 재질문 루프** →
  OR 충족 처리로 **1메시지에 판정 도달** (손목터널증후군 e2e 확인).
- **SSE 실스트리밍**: 응답 완성 후 일괄 표시 → 토큰 단위 표시 (체감 대기 감소, 수치 미측정).
- **인용 환각 차단**: 입력에 없는 chunk_id 인용 제거 + 재시도 (기존 메커니즘, 기록 목적).

## 2026-07-09 — Sprint 33: 다중 실손 판정 + 비교 (L3)

- 예전: 가입현황에서 실손 1건만 선택 → 단건 판정.
- 지금: 여러 실손 다중선택 → 보험별 각각 판정 후 세대·자기부담 비교표 + 비례분담(proration) + 추천.
- 측정: 기능성 변경(라이브 e2e p01 — 삼성4세대 vs 현대3세대 비교표·탭·근거PDF 연동). 정량 지표 비대상.

## 2026-07-09 — Sprint 34: 전 페르소나 대응 + 표준약관 모드 + 가로 크롭

- 예전: 필수 슬롯(insurer/product/incident_date 등)이 다 차야 판정, 부족 시 헤지하며 되묻기(최대 3회).
  가입보험 미상 진입 경로 없음. 가로 2단 약관은 좌측 인용 패널에서 원문이 작아 판독 곤란.
- 지금:
  - **답변-우선**: 결론(가능성 등급)을 첫 문장에 강제, 헤지 선문구 금지, 부족 정보는 맨끝 후속 1회.
    되묻기 임계 3→1. insurer/product/incident_date 를 차단 필수에서 제외(`_COMMON_REQUIRED=("area",)`).
  - **표준약관 모드**: insurer 미상이면 보험사 필터 없는 교차검색 + 결정론 `standard_mode` 프롬프트 주입.
    라이브 e2e — 익명 짧은질문 → "실손 표준약관 기준으로는…" + 보험사 오지칭 **0건**(수정 전 '가입하신
    메리츠화재' 오지칭 → 프롬프트 규칙만으론 누수 → 코드 결정론화로 마감).
  - **가로 크롭**: 삼성 제3조 p.26(가로 2단) → 하이라이트 위치로 기본 단 자동선택, 왼/오른/전체 토글,
    두 단 모두 확대·하이라이트 정합(백엔드 무변경, 프론트 CSS transform).
- 측정: UX/커버리지 변경(정량 지표 비대상). 게이트: pytest 1043→1048 · ruff · tsc · build · Playwright e2e.

## 2026-07-10 — Sprint 36: E2E 정량 지표 (LLM-as-judge)

- **평가셋**: 실손 전용 22문항(`eval/e2e_judge/eval_set.json`) — 6계열(교통사고/질병통원/도수치료/다중비교/익명표준/암입원) + 경계(미용 면책·건강검진·정보부족) + 멀티턴 QA. 스테일 auto/fire 시나리오 3개 삭제.
- **2층 채점**: ① 결정론(코드) — 응답 타입·기대 등급·인용 존재·필수 언급·금지 패턴 ② LLM-as-judge — **AWS Bedrock Claude(오프라인 대조군 전용, 제품 경로 격리)**, 체크리스트 0/1 루브릭·temperature 0. 재현: `python -m eval.e2e_judge.runner`.
- **결과 (2차 확정, 22문항)**:
  | 축 | 지표 | 결과 |
  |:--|:--|:--|
  | 결정론 | 응답 타입 적정 | **22/22 (100%)** |
  | 결정론 | 판정 등급 일치 (기대 명시 문항) | **10/10 (100%)** |
  | 결정론 | 인용 포함 | **22/22 (100%)** |
  | 결정론 | 금지 패턴(오지칭·식별자 누수 등) 위반 | **0건 (100%)** |
  | 결정론 | 필수 키워드 언급 | 12/14 (86%) |
  | judge | 사실 반영 | 17/22 (77%) |
  | judge | 인용 발췌 적합성 | 12/22 (55%) |
  | judge | 톤(근거 없는 확언 없음) | 13/22 (59%) |
  | judge | 기지사실 재요청 없음 | 9/22 (41%) |
- **루브릭 캘리브레이션 (정직 기록)**: 1차 러프에서 judge 축이 과소평가됨(톤 32% 등) — 원인: ① 인용 발췌 280자 절단(뒷부분 질병급여 조항이 잘림) ② '~높은 편입니다' 헤지 톤을 단정으로 오채점 ③ 축 혼입. 발췌 700자 확대 + 톤 정의 명시 + 축 독립 채점으로 보정 → 2차 확정. 1차/2차 모두 results.json 이력 보존.
- **judge 가 정량화한 개선 백로그 (제품 발견 3건)**:
  1. **기지사실 '부족' 언급 잔존(41%)** — 사용자가 이미 준 진단명·입원 사실을 "추가 정보 필요"로 언급하는 패턴. 최대 개선 레버.
  2. **인용 파트 선택(55%)** — 질병 질의에 상해급여-only 발췌, 도수 한도 질문에 한도 조항 미포함 등 주제-청크 정합.
  3. **근거 없는 구체 수치** — "부상등급표 9급/10급" 등 약관 발췌에 없는 수치 생성 사례(톤 축) — 환각 방어 보강 대상.
- **심사용 헤드라인**: "실손 22문항 E2E 에서 판정 등급 일치 100%·인용 포함 100%·금지 위반 0 (결정론 채점). 독립 채점자(Bedrock Claude)가 정성 축을 정량화해 개선 백로그를 도출."

## 2026-07-13 — Sprint 36(계속): 평가셋 고도화 22→42문항 + 인용 결정론 채점 + 일관성 측정

- **평가셋 확장**: 22→**42문항** (`eval_set.json` v2026-07-13) — 기존 6계열 유지 + **경계 면책 8**(임신출산·치과 질병/상해·한방·해외·음주 등) + **발화 변형 6**(만연체·반말·복합질문) + **긴 멀티턴 4**(사실 정정·QA 끼어들기·마음 바꾸기) + **견고성 4**(프롬프트 주입 포함).
- **측정 체계 보강**: ① 인용 **키워드 결정론 채점**(`expected_citation_keywords`, judge 비의존) ② `--repeat N` 등급 일관성(run-to-run) ③ `--only`/`--no-judge` 부분 실행.
- **결과 (42문항 전량, 결정론+judge)**:
  | 축 | 지표 | 결과 |
  |:--|:--|:--|
  | 결정론 | 판정 등급 일치 (기대 명시 17문항) | **17/17 (100%)** |
  | 결정론 | 인용 포함 | **36/36 (100%)** |
  | 결정론 | 인용 조항 키워드 일치 (11문항) | **9/9 (100%)** |
  | 결정론 | 금지 패턴 위반 | **0건** |
  | 결정론 | 응답 타입 적정 | 35/42 (83%) |
  | 결정론 | 필수 키워드 언급 | 18/24 (75%) |
  | judge | 톤 | 35/42 (83%) · 사실 반영 32/42 (76%) · 인용 발췌 적합 22/42 (52%) · 재요청 없음 18/42 (43%) |
- **정직 기록 — 새 스트레스 카테고리가 드러낸 실취약 2건**:
  1. **면책 시나리오에서 판정보다 되묻기 우선** — 미용(쌍꺼풀)·음주·한방 등 면책성 진술에도 "외래 진료 횟수를 알려주세요"류 슬롯 질문으로 응답(type 불일치 7건의 주 원인). 답변-우선 원칙이 면책 판단 경로에는 미적용 — 개선 백로그 #1.
  2. **긴 멀티턴 종반 분류 드리프트** — 사실 정정 후 마지막 턴이 재판정(assessment) 대신 QA(answer)로 분류되는 사례 2건 — classify_intent 백로그 #2.
  - judge 재요청 축(43%)은 마무리 후속 안내문("~알려주시면 더 정확해요")까지 재요청으로 채점하는 루브릭 특성 포함 — 진단축으로만 사용.
- **등급 일관성 (`--repeat 3`, 신설)**: 등급 산출 25문항 × 3회 = 75판정 — 3회 완전 일치 **15/25 (60%)**. 불일치 10건의 분해(정직 기록): **등급 역전(높음↔낮음) 0건** · 인접 등급 흔들림(높음↔중간) 5건 · 되묻기↔판정 갈림 5건(위 실취약 ①과 동근원 — 면책·모호 발화에서 ask/assessment 경계가 run 마다 갈림) · API 타임아웃 1건(측정 노이즈). → 일관성 개선도 백로그 ①(면책 판정-우선)과 함께 잡히는 구조.
- **심사용 헤드라인(갱신)**: "실손 42문항(면책·발화변형·긴멀티턴·주입 견고성 포함) E2E — 판정 등급 일치 100% · 인용 포함 100% · 인용 조항 정합 100% · 금지 위반 0 (결정론 채점). 반복 실행 시 등급 역전 0."
- 재현: `python -m eval.e2e_judge.runner` · 일관성 `--repeat 3` · 부분 `--only <id>`.

## 2026-07-10 — Sprint 35(진행): 하이라이트 정합 + 체감 지연 + 세션 격리

- **가로 2단 하이라이트 (버그 수정)**:
  - 예전: y-앵커 윈도우가 세로 단일컬럼 가정 → 가로 2단(삼성/현대)에서 '왼쪽 단 하단 → 오른쪽 단 상단' 본문 흐름의 오른쪽 단 전체가 오탐 처리. 실측(삼성 제3조 p26): 매칭 단어 64개 중 5개만 통과 → **하이라이트 1박스**.
  - 지금: 단(column)-인지 윈도우(같은 단은 앵커 아래부터, 다음 단은 전체 허용, 이전 단 제외) + 단별 라인 병합(전폭 병합 박스 차단).
- **블록 하이라이트 재작성 (2차 — 사용자 실지적)**:
  - 예전: '청크와 일치하는 단어만' 라인별 병합 → 조사·표기 차이로 문장 중간이 끊긴 누더기 하이라이트("피보험자가 이 특별약관의 보험기간 중 상해 또는"처럼 어절 단위 조각).
  - 지금: **라인별 매칭 밀도(글자 기준 ≥0.5)** 로 인용 본문이 차지하는 연속 라인 구간(run)을 찾고, 구간 내 라인은 **라인 전체 폭**으로 형광펜 블록처럼 도포. 저밀도 1~2라인 갭 허용(표·숫자 라인), 세로 간격 2.5×라인높이 초과 시 런 분리 + 지배 블록(3줄+) 존재 시 고립 1줄 런 제거(페이지 푸터·머리말 오탐 차단).
  - 실측(렌더 육안 + 수치): 한화 3대비급여형 — 조각 3개 → **문단 전체 연속 블록**(푸터 오탐 0) · 메리츠 제4조 — 조항 헤더부터 표 안 본문까지 통 블록 · 삼성 표 청크 — 실제 일치 라인(0.54)만 정직 도포(저밀도 0.14/0.00 라인 제외). 단위테스트 6(합성 2단 PDF) 통과.
- **표 셀-인지 하이라이트 (3차 — 사용자 실지적: 표 안 가로줄)**:
  - 예전: 라인 그룹핑이 같은 y 의 여러 셀(구분|공제금액|보장한도)을 한 줄로 병합 → 표 행을 가로지르는 스트라이프. 원인 실측: 셀 문구가 본문 상용구와 우연 일치(한화 '주사치료를 받아 본인이 실제로 부담한 비급여 의료비' 셀 밀도 0.68 — 해당 청크는 표 본문 미포함).
  - 지금: PyMuPDF `find_tables()` 로 셀 감지 → 표 안 단어는 **셀 단위** 밀도 판정(감지 실패 시 라인 폴백). 셀은 우연 일치 차단 위해 강한 임계(밀도 0.75 + 매칭 8자, 청크가 셀을 실제 포함하면 ~1.0).
  - 실측: 한화 표1 페이지 — 가로줄 오탐 0, 본문 문단+표 제목만 통 블록(청크 내용과 일치). 참고: 문서 추출 자체는 정상(표1 마크다운 온전) — '[보상기간 예시]' 그림표의 셀 병합 반복 추출 흠은 검색 노이즈 수준, 데이터 백로그로 기록.
- **상황→챗 전환 체감 지연**:
  - 예전: 첫 응답이 **완성**될 때까지 로딩 화면 대기. 스텝 애니메이션은 3.6s에 끝나고 이후 응답까지(수십 초) **정지 화면**.
  - 지금: ① SSE 첫 델타 도착 즉시 챗 화면 전환 → 답변이 실시간 타이핑으로 노출. ② 로딩 화면은 마지막 단계 스피너 유지 + 힌트 3.2s 순환(정지 화면 제거).
  - 실측(익명·판정 직행 케이스): 챗 전환 **4.6s**(첫 델타), 최종 판정 카드 7.0s — 이전엔 7s+ 내내 로딩 화면.
- **세션 격리 (버그 수정)**: Welcome에서 새 흐름(정밀/익명) 시작 시 직전 세션이 localStorage 복원으로 이어지던 문제 → startNewSession 강제. 익명 진입이 직전 로그인 컨텍스트(가입 보험·대화)를 노출하던 개인정보 문제 해소.
- **판정 후 멀티턴 (버그 수정)**:
  - 예전: classify_intent 규칙 프리필터가 "슬롯(진단·입원·통원) 존재 → 무조건 재판정"이라, 판정 후 모든 후속 질문('자기부담금이 뭐예요?')이 같은 판정 카드만 반복 — 대화 이어가기 불가. QA 경로도 history 미전달이라 지시형 질문('그게 무슨 뜻?') 불가.
  - 지금: ① 판정 완료(status=answered) 후에는 프리필터 우회 → LLM 이 설명 요청(general_qa)과 사실 정정(재판정)을 분류. ② generate_explanation 에 최근 8턴 history 전달 + 짧은 질문은 직전 안내를 검색 질의에 보강. ③ 세션에 가입 보험사가 있으면 QA 검색을 그 보험사로 스코프(교차검색이 '메리츠화재 약관 기준' 식으로 타 보험사 오귀속하던 문제 — 0건 시 교차 폴백).
  - 실측 4턴: 판정 → '자기부담금?' = answer(삼성 인용만·오귀속 0) → '왜 그렇게 되는 거예요?' = 문맥 이어 답변 → '통원도 2번 했어요' = 슬롯 갱신 재판정. 전부 의도대로.
- **요약 보강**: summary 를 3~4문장 구조로 강제 — ① 결론 ② **인용 조항이 무엇을 정하는지 해설**("근거로 표시된 제3조(보장종목별 보상내용)는 상해로 인한 입원·통원 치료비를 보상한다고 정하고 있어요") ③ 사용자 상황 적용 ④ 부족 시 후속 한 줄. "왼쪽 자료가 그래서 뭔데?"에 요약이 직접 답하게 함. 라이브 실측 확인.
- **본문 내부 식별자 누수 차단 (버그 수정, 사용자 실지적)**:
  - 예전: QA 답변 본문에 "(citation: e8af124a-…, 5d7053cd-…)" chunk UUID 가 그대로 노출 — 프롬프트 금지 규칙(rule 7)만으로는 누수.
  - 지금: `_strip_internal_ids` 결정론 후처리 — UUID 패턴 + "(citation:/chunk_id:/근거:/출처: …)" 괄호 블록 제거 + 잔여 괄호 정리. assessment(summary/satisfied/unsatisfied/next_steps)·answer·help 전 빌더에 적용. 단위테스트 3(실누수 재현 포함). 정상 괄호("공제금액(1~2만원)")는 보존.
  - 라이브 재검: 판정·QA 본문 누수 0.
- **후속 안내 행동형 강제**: "추가 정보가 있어야 정확한 판단이 가능합니다" 식 수동형 마무리 금지 → "'계단에서 넘어진 사고예요'처럼 사고 경위를 알려주시면 바로 다시 확인해 드려요" 행동형(입력 예시 포함)으로. 실측: 부족 정보를 자연어로 던지면 재판정에 반영됨("계단에서 발생한 사고로 인한 입원이므로 보상 요건 충족") — 안내만 없던 것.
- **컨텍스트 전략 3종 실측 비교 — 전 시나리오** (`eval/context_bench/` — 시나리오 6종(교통사고·질병통원·도수치료·다중비교·익명무보험·암입원) × 8턴 × 3전략 = 판정 144회 · 동일 Solar):
  | 지표 (6 시나리오 집계) | S1 2층 기억(현 제품) | S2 전체 이력 상시 | S3 롤링 요약 |
  |:--|:--|:--|:--|
  | 시나리오당 평균 총 프롬프트 토큰 | **1,469** | 5,106 (**3.5×**) | 4,896 (3.3×, 요약콜 12회 포함) |
  | 마지막(8) 턴 평균 프롬프트 | **207 — 평탄** | 1,229 — 선형 증가 | 711 — 톱니 |
  | 평균 지연 | **1.18s** | 1.50s | 1.37s |
  | 최종 답변 사실 명시 (30개) | **26/30 (87%)** | 22/30 (73%) | 20/30 (67%) |
  | 재질문 | 0 | 0 | 0 |
  - 시나리오별 관찰: S1 이 6개 중 4개에서 최고 또는 공동최고. 단 질병통원(S2·S3 5/5)·도수치료(S2 5/5)처럼 S2/S3 이 앞선 케이스도 있음 — 원문 보존의 강점. S3 최악 케이스는 다중비교 **1/5**(요약에서 세대·금액 유실) — 요약 손실 실측.
  - 정직 캐비앗: 시나리오당 1회 실행 · '사실 명시'는 최종 답변 문자 명시 기준(S2 는 문맥에 있어도 정리 답변에서 생략 경향). S1 우위의 핵심은 **비용 구조(평탄 O(1) vs 선형 O(n))** + 일관된 사실 명시. 엑셀 '컨텍스트 전략 비교' 시트에 시나리오별 분해 + 정성 비교 기록.
- **세션 메모 — 2층 기억 (구조 해법)**:
  - 예전: 기억의 정본이 슬롯뿐 → 슬롯 필드에 없는 사실("산재 아님", "가해 차량 있음")은 추출 단계에서 증발, 판정이 맥락을 잃어 케이스별 프롬프트 패치가 반복되는 구조.
  - 지금: extract_slots 가 비슬롯 사실을 `session_notes`(각 60자, 최근 10개 dedupe)로 추출 → `Session.notes` 누적 → **판정·비교·QA 입력에 슬롯과 함께 전달**(프롬프트: 메모 사실 반영 + 재질문 금지). 전체 대화를 매 호출에 넣지 않고도 문맥 유지 — 토큰·PII 통제 유지.
  - 실측: "회사에서 다친 건 아니야" → notes=['산재 아님(회사 밖 사고)'] 기록·판정 전달 확인 (GET /sessions/{id} 디버그 노출). 테스트 +6.
- **대화 연속성 (사용자 실지적: "나 보험 없어" → 상용구 붕괴)**:
  - 예전: classify_intent 가 대화 맥락 없이 텍스트 단독 분류 → 직전 안내("가입 보험사를 알려주시면…")에 대한 답변 "나 보험 없어"가 out_of_domain 상용구("저는 실손 어시스턴트예요…")로 빠져 대화 단절. 판정 요약이 이미 말한 정보(3일 입원)를 다시 요청. 교통사고 도메인 안내 부재.
  - 지금: ① classify_intent 에 **직전 어시스턴트 발화 전달** + 규칙(보험 미가입/미상 진술 = general_qa, 직전 안내에 대한 반응은 문맥 해석) ② QA 프롬프트에 무보험 안내·교통사고 상식(가해자 대인배상 처리 가능, 대인배상·산재 처리분은 실손 제외) ③ 판정 프롬프트에 "이미 밝힌 사실 재요청 금지" + 교통사고 한 줄 안내.
  - 재현 검증(보고된 4턴 그대로): T4 "나 보험 없어" → "가해자 측 자동차보험(대인배상)으로 치료비 처리 가능" 문맥 답변 · T3 교통사고 문맥 유지 · T2 입원 재질문 없음.
- **스트리밍 체감 (2차 — 사용자 실지적: 도움 챗봇 대비 메인 챗이 스트리밍처럼 안 보임)**:
  - 실측: SSE 델타 82개가 **3.2~3.7s 사이 버스트**로 도착(구조화 출력 특성) 후 final 6.2s — 즉시 덤프하면 "한 번에 확 뜨는" 체감.
  - 지금: 스트리밍 버블을 `LiveTypedText`(성장 텍스트를 16ms/3자 속도로 따라가는 타이핑)로 렌더 — 버스트를 매끄러운 타이핑으로 평활화(도움 챗봇과 동일 체감). UI 실측: 타이핑 진행 캡처 확인.
- **인용 페이지 최적 선택 (하이라이트 4차)**: 여러 페이지에 걸친 청크는 시작 페이지에 인용 본문의 알맹이가 없을 수 있음 → hydrate 에서 청크 페이지 범위(최대 4p 스캔) 중 **하이라이트 면적 최대 페이지를 결정론 선택**(cite.page·이미지·박스 일괄 갱신). 스윕 실측: 5개사 7인용 전부 하이라이트 2~21박스(0박스 없음), 현대 p71→p72 이동 사례 확인.
- **마이데이터 표준 API 정합**: 보험업권 표준 규격(`/v2/insu/insurances`·`/basic`) 필드를 테스트베드에서 수집 → RealAdapter 에 **표준응답→InsuranceDict normalizer 실구현**(insu_num/prod_name/insu_status/issue_date/exp_date 매핑 + 실손 세대 결정론 파생: 체결일 ~09.09=1 / ~17.03=2 / ~21.06=3 / 21.07~=4 + 실손·정상계약 필터). 승인 후 `.env` 주소·토큰 2개만 설정하면 더미와 동일 동작(호출부 무변경) — httpx MockTransport 왕복 테스트로 보증. 테스트 +15(경계 7·정규화 5·Real 2 등). 매핑 명세: `docs/infra/mydata-standard-mapping.md`.
- 게이트: pytest **1072** · ruff · tsc · build. 데모 페르소나 11명(+신하율: 가입보험 0건 → 표준약관 시나리오).

---

## 다음 측정 백로그

- [x] ~~검색 골든셋~~ → **30문항 + 회귀 게이트 완료** (Sprint 32 T1, 베이스라인 hit@8=0.833)
- [ ] `rag_score_ratio` 캘리브레이션 + `rag_rerank` on/off A/B (골든셋 기반)
- [ ] IE **실서류 열화 벤치** (저해상 촬영·기울임·도장) — 우위 미입증 상태 해소, 결과에 따라 IE 기본 경로 재검토
- [ ] >1000tok 소프트 상한 재튜닝 (ITEM 패킹 경계)
